# Project Rules

1. Always use HTML validation for form inputs (required, type="email", minlength where needed).

2. Test the application in the browser before committing changes.

3. Use Git branches for each feature or experiment and commit with meaningful messages.